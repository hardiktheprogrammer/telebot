import { IStorageAPI, IUser } from "./types";
declare class StorageAPI implements IStorageAPI {
    constructor();
    Set(user: IUser): Promise<void>;
    Get(id: string | number): Promise<any>;
}
export default StorageAPI;
