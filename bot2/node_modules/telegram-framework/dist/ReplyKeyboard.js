"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Keyboard_1 = require("./Keyboard");
const node_telegram_keyboard_wrapper_1 = require("node-telegram-keyboard-wrapper");
class ReplyKeyboard extends Keyboard_1.default {
    has(button) {
        return this.buttons.includes(button);
    }
    build() {
        const keyboardInstance = new node_telegram_keyboard_wrapper_1.ReplyKeyboard();
        for (let y = 0; y < this.rows; y++) {
            const row = new node_telegram_keyboard_wrapper_1.Row();
            for (let x = 0; x < this.cols; x++) {
                row.push(new node_telegram_keyboard_wrapper_1.KeyboardButton(this.buttons[x + y * this.cols].toString()));
            }
            keyboardInstance.push(row);
        }
        return {
            reply_markup: keyboardInstance.getMarkup()
        };
    }
}
exports.default = ReplyKeyboard;
