import TelegramBot = require("node-telegram-bot-api");
import Route from "./Router";
import { IBot, IBotOptions, IEventCallback, IRouter, IStorageAPI } from "./types";
declare class Bot implements IBot {
    botInstance: TelegramBot;
    storageInstance: IStorageAPI;
    router: Route;
    constructor(token: string, storage: IStorageAPI, options?: IBotOptions);
    use(path: string, router: IRouter): void;
    useMiddleware(...middleware: Array<IEventCallback>): void;
    on(path: string, callback: IEventCallback, ...middleware: Array<IEventCallback>): void;
}
export default Bot;
