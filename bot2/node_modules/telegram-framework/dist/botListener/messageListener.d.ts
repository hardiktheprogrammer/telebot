import TelegramBot = require("node-telegram-bot-api");
import { IBot } from "../types";
declare function messageListener(this: IBot, query: TelegramBot.Message): Promise<void>;
export default messageListener;
