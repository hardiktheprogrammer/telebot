"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class Request {
    constructor(props) {
        this.user = props.user;
        this.message = props.message;
        this.body = props.body;
    }
    match(...cases) {
        let trigger = false;
        for (const item of cases) {
            if (typeof item === 'string' && (item === this.message.text || item === 'default'))
                trigger = true;
            if (Array.isArray(item) && item.includes(this.message.text))
                trigger = true;
            if (typeof item === 'function' && trigger)
                return item();
        }
        return null;
    }
}
exports.default = Request;
