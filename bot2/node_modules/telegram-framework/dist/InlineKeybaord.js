"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Keyboard_1 = require("./Keyboard");
const node_telegram_keyboard_wrapper_1 = require("node-telegram-keyboard-wrapper");
class InlineKeyboard extends Keyboard_1.default {
    has(button) {
        return !!this.buttons.find(e => e[0] === button);
    }
    build() {
        const keyboardInstance = new node_telegram_keyboard_wrapper_1.InlineKeyboard();
        for (let y = 0; y < this.rows; y++) {
            const row = new node_telegram_keyboard_wrapper_1.Row();
            for (let x = 0; x < this.cols; x++) {
                row.push(new node_telegram_keyboard_wrapper_1.InlineKeyboardButton(...this.buttons[x + y * this.rows]));
            }
            keyboardInstance.push(row);
        }
        return {
            reply_markup: keyboardInstance.getMarkup()
        };
    }
}
exports.default = InlineKeyboard;
