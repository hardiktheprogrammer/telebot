import TelegramBot = require("node-telegram-bot-api");
import { IKeyboard, IResponse, IStorageAPI, IUser } from "./types";
import Keyboard from "./Keyboard";
declare class Response implements IResponse {
    private user;
    bot: TelegramBot;
    private storage;
    private query;
    constructor(user: IUser, bot: TelegramBot, storage: IStorageAPI, query: TelegramBot.Message);
    reply(msg: string, keyboard?: IKeyboard): void;
    message(msg: string, options: TelegramBot.SendMessageOptions | Keyboard): Promise<TelegramBot.Message>;
    redirect(path: string): void;
    push(path: string): void;
    pushBack(): void;
}
export default Response;
